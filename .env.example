# Model & server
MODEL_PATH=runs/detect/train/weights/best.pt
MODEL_DEVICE=cpu           # or '0' for GPU 0, 'cpu' for CPU
MODEL_CLASS_NAMES=Health,"carmenta foraseminis","moniliophthora perniciosa","moniliophthora roreri","phytophthora palmivora"
MODEL_NUM_CLASSES=5

# API
API_KEY=change_me_to_secure_value
HOST=0.0.0.0
PORT=8001
WORKERS=1

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:4200

# Logging
LOG_FILE=/var/log/ml_api/predictions.log
